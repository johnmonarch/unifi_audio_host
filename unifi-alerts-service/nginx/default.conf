server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    autoindex off;

    location / {
        try_files $uri =404;
        add_header Cache-Control "public, max-age=60";
    }
}
